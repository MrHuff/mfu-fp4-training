# BF16 control. The launcher removes every low-precision route variable before
# sourcing this file, so an empty numerical treatment is itself the contract.
export USE_FP4_CONVERT_OUTPUT_HEAD='0'
export FP4_KEEP_LAST_N_LAYERS_BF16='32'
export FP4_KEEP_LAST_N_FFNS_BF16='32'
export LBT_REQUIRE_FRESH_START='1'
