# Corrected scaled-RoPE downstream ledger

`DOWNSTREAM_EVAL_LEDGER.csv` contains the nine exact step-38,000 route results
used by the technical report. Every row uses the canonical MMLU 5-shot,
HellaSwag 10-shot, WinoGrande 5-shot, and ARC-Challenge 25-shot panel at seed
42 after canonical conversion/parity validation.

The public artifact contains scientific values only. Checkpoint locations,
metadata inventories, scheduler identities, and operational receipts are not
distributed. `SHA256SUMS` binds the files in this directory.
